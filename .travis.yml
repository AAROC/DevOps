language: bash

before_install:
  - git clone https://github.com/ansible/ansible.git
  - sudo apt-get install python
  - cd ansible/
  - sudo python setup.py install

install:
  - cd $TRAVIS_BUILD_DIR
  - ansible-playbook -i inventories/inventory.identity.dev -C idp-ldap.yml
