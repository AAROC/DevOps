---
- hosts: localhost
  name: Get the required roles
  tasks:
    - git:
        repo: git://github.com/dochang/ansible-role-docker.git
        dest: roles
        accept_hostkey: True

- hosts: localhost
  name: Deploy Discourse with Apache and federated logon
  roles:
    - discourse-sso
