galaxy_info:
  author: Bruce Becker
  description: a role to bring bare metal vms or new machines to a state where we can manage them with Ansible in the Africa-Arabia Regional Operations Centre.
  company: CSIR Meraka Institute
  license: Apache-2.0
  min_ansible_version: 2.0
  platforms:
  - name: EL
    versions:
      - 6
#      - 7
  - name: Ubuntu
    versions:
      - trusty
      - utopic
      - vivid
  - name: Debian
    versions:
      - lenny
      - squeeze
      - wheezy
galaxy_tags: [AAROC, bootstrap]
dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
