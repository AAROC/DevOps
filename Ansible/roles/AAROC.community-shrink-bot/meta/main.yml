galaxy_info:
  author: Bruce  Becker
  description: Role to install the community shrink bot (would you like to talk about that ?)
  company: CSIR Meraka Institute
  license: Apache-2.0

  min_ansible_version: 2.3

  platforms:
    - name: EL
      versions:
      - 7
    - name: Ubuntu
      versions:
        - Xenial

  galaxy_tags:
    - bots
    - slack
    - AAROC

dependencies:
  - AAROC.git
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
