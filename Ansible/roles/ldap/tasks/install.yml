- name: install OpenLDAP server
  yum: name={{ item }}OpenLDAP state=installed
  sudo: True
  with_items:
    - openldap-server 
    - openldap-clients 
    - openldap
  tags: 
    - LDAP
    - install
