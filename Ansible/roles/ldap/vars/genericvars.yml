env:
 RUNLEVEL: 1
# vars file for ldap/
domain: idp.sagrid.ac.za
organisation: SAGrid
admin: admin
Administrator: Administrator
idp_group: idp

# certificate vars
# this may be os dependent - if so, move it to the os-specific vars file
tls_cacert_dir=/etc/openldap/certs 
