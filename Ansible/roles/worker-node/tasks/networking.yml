---
# check the facts for ansible_default_ipv4 - should be the cluster head node IP
- name: check default route
  setup:
  register: default_gw  
  
#- name: set default route
#  shell
  
#- name: set mount points
#te  lineinfile: dest=/etc/fstab line=
