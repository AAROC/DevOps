---
yaim_path: /opt/glite/yaim
yaim_conf_dir: /opt/glite/yaim/etc/{{ site_name }}/siteinfo
yaim_users_home_path: /home
user_home_prefix: ""
# this needs to be set, but will be set to empty - each site should set this in their group_vars
#mysql_pass: ""
bdii_delete_delay: 43200
# service-specific variables
# there is no default, so we set everything off
